import { useEffect, useState } from "react";
import { Heart, Mail, Pencil, Phone, Plus, Search, Trash2, WalletCards, X } from "lucide-react";
import { Button, PageHeader } from "../components/ui";
import { ItemArtwork } from "./campusLifeData";

const apiUrl = import.meta.env.VITE_API_URL || "http://localhost:5000";
const authHeaders = () => ({ "Content-Type": "application/json", Authorization: `Bearer ${localStorage.getItem("authToken")}` });
const categories = ["Books", "Tech", "Furniture", "Fashion", "Academic", "Other"];
const formatPrice = (price) => `৳${new Intl.NumberFormat("en-BD", { maximumFractionDigits: 0 }).format(price)}`;

function ListingModal({ item, onClose, onSave, saving }) {
  const [values, setValues] = useState(() => item ? { title: item.title, price: String(item.price), category: item.category, condition: item.condition || "", description: item.description || "", contactEmail: item.contactEmail || "", contactPhone: item.contactPhone || "" } : { title: "", price: "", category: "Books", condition: "", description: "", contactEmail: "", contactPhone: "" });
  const [error, setError] = useState("");
  useEffect(() => { const escape = (event) => event.key === "Escape" && onClose(); window.addEventListener("keydown", escape); return () => window.removeEventListener("keydown", escape); }, [onClose]);
  const change = (field) => (event) => setValues((current) => ({ ...current, [field]: event.target.value }));
  const submit = (event) => { event.preventDefault(); if (!values.title.trim() || values.price === "" || Number(values.price) < 0) return setError("Enter an item title and a valid price."); onSave({ ...values, title: values.title.trim(), price: Number(values.price), condition: values.condition.trim(), description: values.description.trim(), contactEmail: values.contactEmail.trim(), contactPhone: values.contactPhone.trim() }); };
  return <div className="lost-report-backdrop" role="presentation" onClick={onClose}><section className="lost-report-modal" role="dialog" aria-modal="true" aria-labelledby="listing-title" onClick={(event) => event.stopPropagation()}><button type="button" className="lost-report-modal__close" onClick={onClose} aria-label="Close listing form"><X size={18} /></button><form className="lost-report-form" onSubmit={submit}><header><p className="eyebrow">Marketplace</p><h2 id="listing-title">{item ? "Edit listing" : "Post a listing"}</h2><p>Give other students the details they need to buy your item.</p></header><label><span>Item name</span><input autoFocus value={values.title} onChange={change("title")} maxLength="150" placeholder="e.g. Scientific calculator" /></label><label><span>Price (৳)</span><input type="number" min="0" max="10000000" value={values.price} onChange={change("price")} placeholder="e.g. 1800" /></label><label><span>Category</span><select value={values.category} onChange={change("category")}>{categories.map((category) => <option key={category}>{category}</option>)}</select></label><label><span>Condition <em>Optional</em></span><input value={values.condition} onChange={change("condition")} maxLength="100" placeholder="e.g. Like new" /></label><div className="lost-report-form__contact"><label><span>Email <em>Optional</em></span><input type="email" value={values.contactEmail} onChange={change("contactEmail")} maxLength="254" placeholder="you@example.com" /></label><label><span>Mobile number <em>Optional</em></span><input type="tel" value={values.contactPhone} onChange={change("contactPhone")} maxLength="30" placeholder="e.g. 01XXXXXXXXX" /></label></div><label><span>Description <em>Optional</em></span><textarea value={values.description} onChange={change("description")} maxLength="1000" rows="4" placeholder="Add brand, edition, size, or other useful details" /></label>{error && <p className="lost-report-form__error" role="alert">{error}</p>}<footer><Button type="button" variant="secondary" onClick={onClose} disabled={saving}>Cancel</Button><Button type="submit" disabled={saving}>{saving ? "Saving…" : item ? "Save changes" : "Post listing"}</Button></footer></form></section></div>;
}

export default function MarketplacePage() {
  const [query, setQuery] = useState(""), [filter, setFilter] = useState("All items"), [saved, setSaved] = useState([]), [listings, setListings] = useState([]), [contactDetails, setContactDetails] = useState({}), [editingItem, setEditingItem] = useState(null), [showCreate, setShowCreate] = useState(false), [loading, setLoading] = useState(true), [saving, setSaving] = useState(false), [error, setError] = useState("");
  useEffect(() => { let active = true; fetch(`${apiUrl}/api/marketplace-listings`, { headers: authHeaders() }).then(async (response) => { const result = await response.json(); if (!response.ok) throw new Error(result.message || "Unable to load marketplace listings."); return result.listings; }).then((items) => active && setListings(items)).catch((requestError) => active && setError(requestError.message)).finally(() => active && setLoading(false)); return () => { active = false; }; }, []);
  async function saveListing(values) { setSaving(true); setError(""); try { const isEditing = Boolean(editingItem); const response = await fetch(`${apiUrl}/api/marketplace-listings${isEditing ? `/${editingItem.id}` : ""}`, { method: isEditing ? "PUT" : "POST", headers: authHeaders(), body: JSON.stringify(values) }); const result = await response.json(); if (!response.ok) throw new Error(result.message || "Unable to save your listing."); setListings((current) => isEditing ? current.map((item) => item.id === result.listing.id ? result.listing : item) : [result.listing, ...current]); setShowCreate(false); setEditingItem(null); } catch (requestError) { setError(requestError.message); } finally { setSaving(false); } }
  async function deleteListing(item) { if (!window.confirm(`Delete the listing for “${item.title}”?`)) return; setError(""); try { const response = await fetch(`${apiUrl}/api/marketplace-listings/${item.id}`, { method: "DELETE", headers: authHeaders() }); if (!response.ok) { const result = await response.json(); throw new Error(result.message || "Unable to delete this listing."); } setListings((current) => current.filter((listing) => listing.id !== item.id)); } catch (requestError) { setError(requestError.message); } }
  async function showContact(item) { if (item.canEdit || contactDetails[item.id]) return; try { const response = await fetch(`${apiUrl}/api/marketplace-listings/${item.id}/contact`, { headers: authHeaders() }); const result = await response.json(); if (!response.ok) throw new Error(result.message || "Unable to load seller contact details."); setContactDetails((current) => ({ ...current, [item.id]: result })); } catch (requestError) { setError(requestError.message); } }
  const items = listings.filter((item) => `${item.title} ${item.category} ${item.description}`.toLowerCase().includes(query.toLowerCase())).filter((item) => filter === "All items" || (filter === "Under 2000" ? item.price < 2000 : item.category === filter));
  const toggleSaved = (id) => setSaved((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id]);
  return <section className="page campus-page"><PageHeader eyebrow="Campus life" title="Student marketplace" description="Pass on what you no longer need and find what makes student life easier." actions={<Button onClick={() => { setEditingItem(null); setShowCreate(true); }}><Plus size={17} /> Post a listing</Button>} /><section className="marketplace-intro"><div className="marketplace-intro__icon"><WalletCards size={29} /></div><div><span className="event-hero__eyebrow">Buy local, keep it circular</span><h2>Good things deserve a second semester.</h2><p>Browse trusted listings from students on your campus.</p></div><div className="marketplace-intro__stats"><strong>{listings.length}</strong><span>active listings</span></div></section><div className="marketplace-controls"><label className="event-search"><Search size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search books, tech, furniture…" /></label><div className="filter-pills marketplace-pills">{["All items", "Books", "Tech", "Under 2000"].map((item) => <button className={`filter-pill ${filter === item ? "is-active" : ""}`} type="button" key={item} onClick={() => setFilter(item)}>{item}</button>)}</div></div>{error && <p className="lost-page-error" role="alert">{error}</p>}{loading ? <p className="lost-page-empty">Loading listings…</p> : <div className="marketplace-grid">{items.map((item) => <article className={`market-item ${item.canEdit ? "market-item--owner" : ""}`} key={item.id} onClick={() => showContact(item)}><ItemArtwork label={item.category.slice(0, 4).toUpperCase()} hue="violet" /><button type="button" className={`market-save ${saved.includes(item.id) ? "is-saved" : ""}`} onClick={(event) => { event.stopPropagation(); toggleSaved(item.id); }} aria-label={`Save ${item.title}`}><Heart size={17} fill={saved.includes(item.id) ? "currentColor" : "none"} /></button><div className="market-item__body"><span className="tag">{item.category}</span><h2>{item.title}</h2><p>{item.condition || item.description || "No condition details provided"}</p>{item.description && item.condition && <p>{item.description}</p>}<div><strong>{formatPrice(item.price)}</strong><span>by {item.sellerName}</span></div>{contactDetails[item.id] && <div className="market-contact"><strong>Seller contact</strong>{contactDetails[item.id].contactEmail && <a href={`mailto:${contactDetails[item.id].contactEmail}`} onClick={(event) => event.stopPropagation()}><Mail size={14} /> {contactDetails[item.id].contactEmail}</a>}{contactDetails[item.id].contactPhone && <a href={`tel:${contactDetails[item.id].contactPhone}`} onClick={(event) => event.stopPropagation()}><Phone size={14} /> {contactDetails[item.id].contactPhone}</a>}{!contactDetails[item.id].contactEmail && !contactDetails[item.id].contactPhone && <span>No contact details shared.</span>}</div>}{item.canEdit && <div className="market-owner-actions"><button type="button" onClick={(event) => { event.stopPropagation(); setEditingItem(item); setShowCreate(true); }}><Pencil size={14} /> Edit</button><button type="button" className="is-delete" onClick={(event) => { event.stopPropagation(); deleteListing(item); }}><Trash2 size={14} /> Delete</button></div>}</div></article>)}</div>}{!loading && !items.length && <div className="no-results">No listings match that search. Try a different keyword.</div>}{showCreate && <ListingModal item={editingItem} onClose={() => { setShowCreate(false); setEditingItem(null); }} onSave={saveListing} saving={saving} />}</section>;
}
